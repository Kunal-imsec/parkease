# Production Configuration
server.port=${PORT:8080}
spring.application.name=parkease-backend

# JWT Configuration
jwt.secret=${JWT_SECRET:parkease-secret-key-for-jwt-token-generation-minimum-256-bits}
jwt.expiration=86400000

# Supabase PostgreSQL Database Configuration
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://db.bxidlvpleddpwflfcqpw.supabase.co:5432/postgres}
spring.datasource.username=${DATABASE_USERNAME:postgres}
spring.datasource.password=${DATABASE_PASSWORD:6x#&3mUTPyQ#+/m}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# CORS Configuration
cors.allowed.origins=${CORS_ORIGINS:https://parkease.vercel.app}

# Logging
logging.level.com.parkease=INFO
logging.level.org.springframework.security=WARN

