# Server Configuration
server.port=8080
spring.application.name=parkease-backend

# JWT Configuration
jwt.secret=parkease-secret-key-for-jwt-token-generation-minimum-256-bits
jwt.expiration=86400000

# Supabase PostgreSQL Database Configuration
# Supabase Transaction Pooler (IPv4 - Render Compatible)
spring.datasource.url=jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:6543/postgres?user=postgres.bxidlvpleddpwflfcqpw&password=6x%23%263mUTPyQ%2B%23%2Fm
spring.datasource.driver-class-name=org.postgresql.Driver


# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# CORS Configuration
cors.allowed.origins=http://localhost:5173,http://localhost:3000

# Logging
logging.level.com.parkease=DEBUG
logging.level.org.springframework.security=DEBUG

